apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ctf-env-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ctf-env-client-service
            port:
              number: 80